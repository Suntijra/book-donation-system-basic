// require('dotenv').config();
// const { DB_HOST, DB_USER, DB_PASSWORD, DB_DATABASE } = process.env;

const pool = {
    host: '127.0.0.1',
    user: 'root',
    password: 'Saknsaza123=',
    database: 'book_donation_center',
}

module.exports = pool;
